# Exclude compiled files in api/ directory
# Vercel will compile the TypeScript files itself
api/*.js
api/*.js.map
api/*.d.ts
api/*.d.ts.map

# Exclude compiled output directory
dist/

# Exclude node_modules (Vercel installs its own)
node_modules/

# Exclude tests
tests/
*.test.ts

# Exclude local Vercel files
.vercel/
